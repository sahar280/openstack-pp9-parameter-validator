# Security and Disclosure

Do not attach real command output, hostnames, IP addresses, serial numbers,
credentials, customer identifiers, or internal incident data to public issues.

The public script uses placeholders for infrastructure tuning values. Do not
replace them in the repository with production values. Report security concerns
privately to the repository owner.
